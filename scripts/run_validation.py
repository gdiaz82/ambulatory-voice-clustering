#!/usr/bin/env python
"""Thin wrapper: score the clustering or run a search (``--search``). See ``avc-validate``."""

from voice_clustering.cli import validate_main

if __name__ == "__main__":
    validate_main()
